import "package:flutter/material.dart";

class FontSelect extends StatelessWidget {
  const FontSelect({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("폰트 선택하기"));
  }
}
